{
  "name": "jevkit-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root for the jevkit packages. Not published; the published umbrella is packages/jevkit.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0"
  },
  "packageManager": "pnpm@8.15.0"
}
